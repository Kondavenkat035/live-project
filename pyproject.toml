[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "live-project"
version = "0.1.0"
description = "Sample Python project for Jenkins CI pipeline"
authors = [
  { name="Konda Venkat", email="example@email.com" }
]

dependencies = [
  "flask"
]
